Problem Statement: This application connects with PostgresDB and generates CSV file for given table. if you see my CsvGenerator class code, it uses currentdate property value(i.e. CURRENT_DATE) from application.yml and use it in SQL query in code. my spring boot application runs as Cronjob in k8s env,it also uses values-<env>.yml file for diff environment like dev, uat etc. The Problem is if this cronjob not able to Run OR Job runs and endup in error then next day we have to manually create and run the Cronjob again by updating helm values (i.e. currentdate: CURRENT_DATE-1) I want you give me a solution where i dont need to create/run manual cronjob in k8s env by updating helm currentdate values. Please share the solution by sharing code/config whatever.



Ok.. ive a new Input. My Job will run k8s env, which runs in UTC time zone. Say my Job will run 19:30 UTC on Monday, means if Job runs within 1930-23:59 UTC then currentdate value(CURRENT_DATE) will be interpreted as say 2025-07-25.. BUT if Job unable to run/or execution with error, means we have to run this job again(since there are manual retry mechanism.. thats OK and expected). Now if we run this Job after 0:00 UTC Tuesday(2025-07-26) then currentdate value will be interpreted as 2025-07-26, which is wrong because, we are unable to share the data for prevDay i.e. Monday(2025-07-25). So Requirement is that even a normal CronJob runs fails, it should always run with prev currentdate i.e. CURRENT_DATE-1 next day during manual cronjob rub without updating any code/config values. Hope you understand the Issue. Please share the changes and suggestions.
